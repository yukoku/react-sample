import { TIMER_ACTIONS } from '../Actions/index'

const initialState = {
}

const trelloReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default trelloReducer;
